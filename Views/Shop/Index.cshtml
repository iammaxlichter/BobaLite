@model List<BobaLite.Models.Drink>

<h1>Our Drinks</h1>

@foreach (var drink in Model)
{
    <h2>@drink.Name</h2>
    <ul>
    @foreach (var variant in drink.Variants)
    {
        <li>@variant.Name — $@variant.Price (@variant.Quantity cans)</li>
    }
    </ul>
}
